import React from "react";

export default function Phonetics(props) {
    if (props.phonetics) {
        console.log(props.phonetics);
        return null;
    }
    else {
        return null;
    }
}